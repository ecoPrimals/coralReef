[package]
name = "coral-driver"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Sovereign GPU driver — DRM ioctl dispatch for AMD (amdgpu) and NVIDIA (nouveau)"

[dependencies]
thiserror.workspace = true
tracing.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
# No external crate dependencies for ioctl — pure Rust via libc syscall wrappers.
# All DRM structures are defined internally.

[dev-dependencies]
